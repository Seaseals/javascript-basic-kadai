// 体重、身長の宣言
let userWeight;
let userHeight;

// 値の代入
userWeight=68;
userHeight=1.7;

let bmi = userWeight / (userHeight * userHeight);
console.log(bmi);
// コンソールへの出力
// console.log(userWeight/ (userHeight * userHeight) );